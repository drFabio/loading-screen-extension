{
  "name": "loading-screen-ext-react",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel",
    "prebuild": "rm -rf dist/*",
    "build": "parcel build  src/**/*.html src/**/*.tsx src/**/*.ts",
    "copy:static_files": "cp public/* dist/",
    "postbuild": "npm run copy:static_files"
  },
  "author": "",
  "license": "UNLICENSED",
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.0.25",
    "@types/react-dom": "^18.0.9",
    "global": "^4.4.0",
    "parcel": "^2.8.0",
    "typescript": "^4.9.3"
  }
}
